<h2>商品一覧</h2>

<% @items.each do |item| %>
  <%= link_to public_item_path(item.id) do %>
    <%= image_tag item.image, size: '220x140' %>
  <% end %>
  <%= link_to item.name, public_item_path(item.id) %>
  <%= item.price %>

<% end %>
<%= paginate @items %>